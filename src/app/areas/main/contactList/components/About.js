import React, { PropTypes } from 'react'

const About = (issue, uiState) => (
  <div>
    About
  </div>
)

About.propTypes = {
  issue: PropTypes.any.isRequired,
  uiState: PropTypes.any.isRequired
}

export default About
