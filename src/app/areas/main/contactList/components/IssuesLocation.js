import React, { PropTypes } from 'react'

const IssuesLocation = (uiState) => (
  <div className="issues__location">
    address
  </div>
)

IssuesLocation.propTypes = {
  uiState: PropTypes.any.isRequired
}

export default IssuesLocation
